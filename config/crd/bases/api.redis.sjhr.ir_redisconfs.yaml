---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: redisconfs.api.redis.sjhr.ir
spec:
  group: api.redis.sjhr.ir
  names:
    kind: RedisConf
    listKind: RedisConfList
    plural: redisconfs
    singular: redisconf
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RedisConf is the Schema for the redisconfs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RedisConfSpec defines the desired state of RedisConf
            properties:
              imageTag:
                description: Foo is an example field of RedisConf. Edit redisconf_types.go
                  to remove/update
                type: string
            required:
            - imageTag
            type: object
          status:
            description: RedisConfStatus defines the observed state of RedisConf
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
